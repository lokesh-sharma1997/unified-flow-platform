spring:
  application:
    name: ufp-mlops
  datasource:
    url: jdbc:postgresql://localhost:5432/ufp_mlops
    username: ufp
    password: ufp
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8083

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.ufp: DEBUG
