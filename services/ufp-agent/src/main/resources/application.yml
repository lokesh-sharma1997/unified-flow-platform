spring:
  application:
    name: ufp-agent
  datasource:
    url: jdbc:postgresql://localhost:5432/ufp_agent
    username: ufp
    password: ufp
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.ufp: DEBUG
